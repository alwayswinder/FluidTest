# FluidTest 项目长期记忆

## 项目技能位置（重要）
- 项目权威技能不在 `~/.workbuddy/skills/` 或 `.workbuddy/skills/`，而在 **`.dsh/skills/`**（DSH = `@deepseek-ai/dsh`，`npx dsh web` 跑在 3080 端口，启停脚本在 `Docs/start-dsh.bat` 等）
- 4 个技能：`blueprint-to-cpp-migration`（迁移唯一权威规范）、`mcp-automation-bridge`（WebSocket 8090/8091 控制编辑器）、`special-agent-mcp`（SpecialAgent MCP 8767 端口）、`user-preferences`（中文提交/注释/交流）
- 需要时直接 Read 对应 `SKILL.md` 遵守其规则；DSH 技能不在 WorkBuddy Skill 工具列表中，无法用 Skill 调用

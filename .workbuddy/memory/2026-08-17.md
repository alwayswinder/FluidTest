# 2026-08-17 工作日志

## 查询 NinjaLiveComponent 蓝图 AutonomousMode 变量用途
- 通过 McpAutomationBridge WebSocket(8091) `manage_blueprint` 读取蓝图信息（技能文档提到用 manage_blueprint 读图，实测 `action: 'get'` + `blueprintPath` 可返回全部 330 个变量定义；遍历 EventGraph(643 节点) 找变量引用）
- 结论：`AutonomousMode`（bool，默认 True，分类 NONPUBLICLiveSystem）控制 BeginPlay 初始化时**预设加载方式**：
  - True（自主模式）：检查 DefaultPreset 是否有效，有效则自动 Set ActualPreset，并继续初始化序列（CreateDynamicMaterialInstances、GetAllActorsWithInterface→NinjaLIVETraceExclude、ManageContinuousInteractions）
  - False（手动模式）：走 MenuSelection_Bind_n_Trigger_2 / PresetSelection_Bind_n_Trigger_2 菜单预设选择流程，等用户选预设后才继续
- 链路：Event BeginPlay → MemoryManagerConnection → CreateOrAcquireRenderTargets → Check Preset Manager → Branch(Condition=AutonomousMode)
- 数据缓存：`Saved/Temp/mcp_query/bp_vars.json`（330 变量定义）、`eventgraph_full.json`（EventGraph 全节点）——迁移预设逻辑时可直接复用

## 迁移 CheckComponentOwner 复合节点 → MyCheckComponentOwner
- 用户提供 CheckComponentOwner 折叠图完整序列化数据，据此还原语义：
  - Branch: Condition = ClassIsChildOf(GetObjectClass(GetOwner()), NinjaLive_C)
  - true → Cast to NinjaLive_C → 输出 exec `OwnerIsNinjaLiveClass` + object `AsNinjaLive`（CastFailed 未连接，冗余）
  - false → 输出 exec `OwnerNotNinjaLiveClass`
- C++ 实现 `UMyNinjaLiveComponent::MyCheckComponentOwner(AMyNinjaLiveActor*& AsNinjaLive) const`（头文件+源文件已改）：
  - 返回 bool 映射两个 exec 出口（true=OwnerIsNinjaLiveClass / false=OwnerNotNinjaLiveClass）
  - 用 IsA<AMyNinjaLiveActor> 代替蓝图类 NinjaLive_C 判断（语义等价，与 MyEnableOwnerInput 一致）
  - 已加 AMyNinjaLiveActor 前向声明；Category=FluidSim|Input
- 状态：**代码已写，未编译**（编辑器运行中，需关闭后 Build.bat）
- AGENTS.md 已迁移清单已同步更新

## 注释风格调整（用户需求）
- 用户反馈：注释过多且重复（尤其"已核对完整节点数据"的逐节点还原注释），复杂蓝图迁移后注释可能多过代码
- 需求：注释只写功能说明（一句话），暂时不确定/不准确的没关系，随迁移补充完善
- 已简化：`MyNinjaLiveComponent.h/cpp`、`MyNinjaLiveActor.h/cpp`、`MyNinjaFluidEnums.h` 全部重写注释，删掉所有节点级还原（grep 验证无残留），头文件 269→185 行
- 已把「注释规范」写入 `.dsh/skills/blueprint-to-cpp-migration/SKILL.md`（§4 新增小节，含反例/正例）和 `AGENTS.md` 迁移规范
- 后续所有迁移代码一律遵守：一句话功能注释，不粘贴蓝图节点还原

## 蓝图文本导出压缩工具（可复用）
- 任务：把 `Docs/tmp.txt`（UE 蓝图 Composite 子图 `ProximityActivation-MasterVars-Quantizer-OutMat` 序列化文本，180KB/724行/74节点）压缩为功能提取
- 脚本：`Docs/extract_bp.py`（Python 标准库，managed python 3.13 直接跑）
- 关键坑：每个 Pin 行含嵌套 `()`（PinType 字段），正则必须用**行锚定** `^\s*CustomProperties Pin \((.*)\)\s*$`（re.M，不加 re.S）才能抓到整行，否则会在第一个 `)` 截断 → 丢失全部 `LinkedTo` 连线
- 压缩策略：丢弃冗余 boilerplate（PinId GUID、PersistentGuid、14 个恒定 False/None 的 PinType 子字段、ExportPath、布局坐标可省）；保留语义（节点类型、函数/变量名、每条 Pin 连线边的 node.pin 映射、条件、字面常量、设计注释原文）。连线用 PinId→(node,pinname) 全局映射还原为可读 `节点.引脚`
- 产物：`Docs/tmp_extracted.md`，约 18KB（**体积 -90%**），含 设计注释 / 派生速查表(写/读变量、调用函数、Cast、运算) / 节点图(全连线)
- 经验：UE 文本导出里 `\r\n` 是字面两字符，需 `.replace("\\r\\n"," ")` 才能正确显示换行

## 迁移 ProximityActivation-MasterVars-Quantizer-OutMat 复合节点
- 数据源：`Docs/tmp_extracted.md`（extract_bp.py 产物）+ `Docs/tmp.txt` 原始序列化交叉验证
- 语义还原（74 节点，5 段逻辑）：
  1. In1 入口（FromOwner）：CheckComponentOwner → 读 Owner 的 Disable/Proximity 设置；Disable=true 只设标志返回；Proximity=true 抑制 BeginPlay（等 Pawn 靠近再激活）
  2. In2 入口（直接）：量化修正 → 双缓冲 → MasterVars → OutMat 占位
  3. 量化修正：TraceMeshMovingInWorldSpace > 3（Step1m+）且 CameraFacing 时强制切回 Automatic（两者不兼容）
  4. 双缓冲：UsePAINTER_V2 且非 SingleTargetMode_LEGACY 时启用
  5. MasterVars：InitDone/MaterialInstacesDone=false、UE5EAFLAG=!Contains("EarlyAccess")、QuantizerStepSize=MyQuantizerValues()；OutMat 空数组补 M_Null 占位材质
- 新增 15 个组件变量（_V2_ToTrackObjects/MyCameraFacingTraceMesh/MySingleTargetMode_LEGACY）+ 5 个函数（主流程/FromOwner/量化修正/双缓冲/OutMat 占位）
- 注意：提取文件把 IfThenElse_29.then 误标为连 IfThenElse_2，原始数据证实连 IfThenElse_3（Proximity=true 分支）——已按原始数据实现
- 状态：**代码已写，未编译**（需关闭编辑器 Build.bat）

### 用户修正（2 轮）：
1. `MyComponentActivatedByPawnProximity` / `MyDisableComponent` 应为**临时变量**，不从组件读入——改为函数内直接从 `NinjaLive->MyDisableBlueprint` / `MySimActivatedByPawnProximity` 读取判断，删掉 2 个成员
2. 不拆分多个子函数，**尽量与蓝图一致**——蓝图是一个复合节点（In1/In2 双入口 + Out），删除 3 个子函数（量化修正/双缓冲/OutMat 占位），逻辑内联进 2 个入口函数 `MyProximityActivationMasterVarsQuantizerOutMat`（In2）/ `MyProximityActivationMasterVarsQuantizerOutMatFromOwner`（In1）
